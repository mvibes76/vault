# Video Vault v30 Notes

## Fix
- Folder slideshow/gallery display toggle now works in every folder, not only nested folders marked as galleries.
- Any active folder can switch between Grid and Slideshow using the header button.
- The slideshow renderer now respects each folder's `display_mode` consistently while keeping Expand all as a flat grid/wall behavior.

## Supabase
No schema update required.
